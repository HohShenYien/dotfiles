## ZSH

My .zshrc, nothing too fancy here. Before replacing the file, these libraries should be installed:

```bash
paru -S pyenv zoxide direnv eza zsh-syntax-highlighting ttf-meslo-nerd-font-powerlevel10k zsh-theme-powerlevel10k nvm
```

```bash
paru -S zip unzip
curl -s "https://get.sdkman.io" | bash
```
